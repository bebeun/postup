<% (xx.class.name == "User") ? (name = xx.name) : (name = xx.profile.class.name+" : "+xx.profile.description) %>
<% if user_signed_in? %>
	<% if current_user == xx %>
		<% name = "You" %>
	<% end %>
<% end %>
<%= (name == "You") ? (name) : (link_to(name, xx)) %>


