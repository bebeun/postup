<h2>New Call Partial --------------------------- </h2>

<% if @call.errors.any? %>
	<%= pluralize(@call.errors.count, "error") %> prohibited this conversation from being saved:
	<ul>
		<% @call.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
	</ul>
<% end %>



<%= form_tag(calls_path) do %>

	Select the Name of the user you want to call out: <br/>
	<%= select_tag 'call[global_id]', \
	options_for_select(@User.collect{|p| [ "USER : "+p.name+" : "+p.profiles.collect{|x| x.class.name+" "+x.description }.join(", "), get_user_global_id(p) ]} + \
	@PotentialUser.to_a.collect{|p| [ "POTENTIAL USER : "+p.profile.class.name+" : "+p.profile.description, get_user_global_id(p) ]}), \
	prompt: "Select User", \
	:onchange => "this.form.submit()"  %>
<% end %>




<%= form_tag(calls_path) do %>
	Or enter a new profil URL <br/>
	(exemple: www.facebook.com/bebeun92 or www.twitter.com/benoit81) <br/>
	<%= text_field_tag 'call[display]', 'Entire a new profile URL...' %>
	
	 <%= submit_tag 'Submit' %>
	<br/>
<% end %>

<h2>--------------------------------------------- </h2>